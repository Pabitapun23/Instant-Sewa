class Provider {
  String name;
  String job;
  String image;
  String desc;
  int rate;
  double distance;

  Provider(
      {this.name, this.job, this.image, this.desc, this.rate, this.distance});
}
