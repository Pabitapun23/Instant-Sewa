import 'package:flutter/material.dart';

class Profile{        //model = database
  String name;
  String username;
  String img;
  String address;
  String phone_number;
  String email;

  Profile({this.name, this.username, this.img, this.address, this.phone_number, this.email});         //{} optional
}
