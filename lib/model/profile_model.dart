import 'package:flutter/material.dart';

class Profile{        //model = database
  String name;
  String img;
  String address;
  String phone_number;
  String email;

  Profile({this.name, this.img, this.address, this.phone_number, this.email});         //{} optional
}
